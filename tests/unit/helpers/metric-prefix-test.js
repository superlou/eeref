import { metricPrefix } from 'eeref/helpers/metric-prefix';
import { module, test } from 'qunit';

module('Unit | Helper | metric prefix');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = metricPrefix([42]);
  assert.ok(result);
});
